{
  "$comentario": "As duas listas de excecao NOMINAL do projeto. Lei dona: specs/arquitetura/04-regras.md §6. 'excecoes' e ao catalogo do gate: modulo + regra + motivo + decisao. 'excecoesCve' e da tools/ci-dependencies.mjs (nao roda no gate; dono e a ferramenta, nao esta regra) — exige tambem 'expira'. As DUAS exigem 'decisao' apontando para um ADR de specs/adr/: quem resolve se o ADR EXISTE de verdade e a regra (04-regras.md §6, contra o fail-open medido em 2026-08-15), este schema so cobra a FORMA — string presente, nunca vazia. Chave em ingles ('module'/'rule') ou campo a mais reprova aqui por additionalProperties:false — e o defeito medido: exececao com chave errada falhava calada, sem esta regra apontar o motivo.",
  "type": "object",
  "additionalProperties": false,
  "required": ["excecoes", "excecoesCve"],
  "properties": {
    "excecoes": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["modulo", "regra", "motivo", "decisao"],
        "properties": {
          "modulo": { "type": "string", "minLength": 1 },
          "regra": { "type": "string", "minLength": 1 },
          "motivo": { "type": "string", "minLength": 1 },
          "decisao": { "type": "string", "minLength": 1 }
        }
      }
    },
    "excecoesCve": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "motivo", "decisao", "expira"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "motivo": { "type": "string", "minLength": 1 },
          "decisao": { "type": "string", "minLength": 1 },
          "expira": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" }
        }
      }
    }
  }
}
